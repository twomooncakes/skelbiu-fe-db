import Login from "../components/user/Login";

function LoginPage() {
    return (
        <main>
            <h1 className="page-title">Login</h1>
            <Login />
        </main>
    );
}

export default LoginPage;